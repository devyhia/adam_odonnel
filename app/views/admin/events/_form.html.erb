<style type="text/css">
.field {
  line-height: 2.5em;
}
</style>
<%= form_for(@admin_event) do |f| %>
  <% if @admin_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_event.errors.count, "error") %> prohibited this admin_event from being saved:</h2>

      <ul>
      <% @admin_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :what %><br>
    <%= f.text_field :what, :class=>"form-control" %>
  </div>
  <div class="field">
    <%= f.label :what_more %><br>
    <%= f.text_area :what_more, :class=>"form-control" %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image, :class=>"form-control" %>
  </div>
  <div class="field">
    <%= f.label :when %><br>
    <%= f.datetime_select :when, :class=>"form-control" %>
  </div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-danger" %>
  </div>
<% end %>
